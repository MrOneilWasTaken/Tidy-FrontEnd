export default function Modals(props){
  // Mode modal into here take the button ID as an arguement and save it into the tasks ID as dayID 
  // Maybe this is a solution that doesn't copy code.
  return(
    <>

    </>
  )
}