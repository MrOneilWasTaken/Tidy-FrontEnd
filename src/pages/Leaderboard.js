export default function Leaderboard() {
  // Doing an API call to the Leaderboard Database
  // *USER ID* | POSITIONONTABLE | USERNAME | TASKSCOMPLETED *DCS ORDER*

  return (
    <>
      <h1>Leaderboard</h1>
      <ul>
        <li>(Position) | (Username) | (Tasks Completed)</li>
        {/* Map users onto leaderboard in list elements */}
        
      </ul>
    </>
    )
}